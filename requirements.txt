# GhostScripter-K1-K2 Requirements
# GPL-3.0 License

# GUI Framework
# qtpy provides a backend-agnostic Qt abstraction layer (PyQt5 / PyQt6 / PySide6)
# Install your preferred backend separately; PyQt5 is the current default.
qtpy>=2.4.3
PyQt5>=5.15.0         # default backend — swap for PyQt6>=6.4 or PySide6>=6.4 freely
PyQtWebEngine>=5.15.0

# Database
SQLAlchemy>=2.0.0

# Configuration
PyYAML>=6.0
jsonschema>=4.0.0

# IPC/REST
requests>=2.28.0
flask>=2.3.0

# MCP server (the AI-agent backend — python -m ghostscripter.mcp)
mcp>=1.2.0
uvicorn>=0.30.0       # only needed for --mode http / --mode sse

# Dialogue graph analysis (DialogueFile.to_networkx, cycle detection)
networkx>=3.0

# Texture preview (TGA decoding in the Asset Library)
Pillow>=10.0.0

# Utilities
colorama>=0.4.6
python-dotenv>=1.0.0

# ── KotOR I/O library ────────────────────────────────────────────────────────
# PyKotor is the canonical KotOR binary-format library (OldRepublicDevs/PyKotor).
# Required for: compileScript, decompileScript, writePTH (real GFF binary output),
# and opportunistic readIFO/readNCS delegation.
#
# Install from PyPI (recommended):
#   pip install pykotor
# Or from the PyKotor monorepo (picks up latest fixes):
#   pip install -e Libraries/PyKotor
pykotor>=2.3.0

# Development
pytest>=7.0.0
pytest-cov>=4.0.0
